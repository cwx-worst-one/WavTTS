---
################################################################################
##########                       BASIC CONFIGURATION                  ##########
################################################################################

seed: 1996
__set_seed: !apply:pytorch_lightning.seed_everything [!ref <seed>]

################################################################################
##########                    TRAINING CONFIGURATION                  ##########
################################################################################
run_opts:
    log_dir: logs
    hdfs_path:
    fast_dev_run: False
    tensorboard_dir: !ref <run_opts[log_dir]>
    log_name: unified_diffusion
    version: 1.0
    precision: bf16
    accelerator: auto
    strategy: ddp_find_unused_parameters_false # deepspeed_stage_2_offload, ddp_find_unused_parameters_false
    cache_dir: ./.cache_dir
    profiler: simple
    flash_attn: null
    num_epochs: 100
    batch_size: 16
    num_workers: 15
    learning_rate: 0.0001
    weight_decay: 0.001
    checkpointing: False
    n_step_save: 10000
    batch_total_tokens: 30000
    urls: null
    data_id: null
    val_data_id: 969
    accumulate_grad_batches: 1
    resume_ckpt_path:
    use_url_lst: False
    target: bn
    prompt_feature: bn
    ctx_feature: bn
    use_textprefix: False
    use_prompt: False
    use_unet_style_skip_connect: true
    asset_dir: recipes/voicebox/assets
    val_output_samples_dir: generated_samples
    val_check_interval: 20000
    limit_val_batches: 1

model_config: !new:recipes.voicebox.diffusion_modules.llama_ldm.ModelArgs
    in_channels: 64
    out_channels: 64
    prompt_mel_dim: 64
    n_token: 32768 #TODO check latest value
    token_upscales: [2, 2, 2]
    token_downscales: [5]
    llama_provider: ctiga
    target: !ref <run_opts[target]>
    prompt_feature: !ref <run_opts[prompt_feature]>
    ctx_feature: !ref <run_opts[ctx_feature]>
    local_cond_dim: 256
    time_embed_dim: 256
    encoder_dim: 1024
    encoder_n_layers: 24
    encoder_n_heads: 16
    use_prompt: !ref <run_opts[use_prompt]>
    use_textprefix: !ref <run_opts[use_textprefix]>
    use_unet_style_skip_connect: !ref <run_opts[use_unet_style_skip_connect]>

mel_config: # 40hz iBigVGAN
    n_fft: 4096
    num_mels: 100
    hop_size: 600
    win_size: 2400
    fmin: 0
    fmax: 12000
    sampling_rate: 24000

bn_config: # 40hz wvae 3.1
    hop_size: 600
    bn_norm_std: 15
    bn_norm_mean: 0
    bn_padding: -5
    pad_trim: False
    silence_bn_path: recipes/voicebox/vocoder/silence_wvae/v3_40hz/silence_wvae.npy

train_dataset:
    !new:recipes.voicebox.datasets.voicebox_promptmel_dataset.VoiceBoxParquetDataset
    data_id: !ref <run_opts[data_id]>
    drop_last: False
    batcher_config:
        buckets: !apply:eval ["list(range(40, 2500, 60))"]
        dynamic_batch: True
        maximum_bucket_size: !ref <run_opts[batch_total_tokens]>
        length_fn: !apply:eval ['lambda x: x["bn"].shape[1]'] # seq.shape
    mel_config: !ref <mel_config>
    max_length: 60
    mel_norm_mean: -2.5
    mel_norm_std: 6
    mel_padding: -5
    use_text: !ref <run_opts[use_textprefix]>
    use_bn: True
    bn_config: !ref <bn_config>
    mask_use_alignment: False
    umm_hop_size: 960

val_dataset:
    !new:recipes.voicebox.datasets.voicebox_promptmel_dataset.VoiceBoxParquetDataset
    data_id: !ref <run_opts[val_data_id]>
    drop_last: False
    batcher_config:
        buckets: !apply:eval ["list(range(40, 2500, 60))"]
        dynamic_batch: True
        maximum_bucket_size: !ref <run_opts[batch_total_tokens]>
        length_fn: !apply:eval ['lambda x: x["bn"].shape[1]'] # seq.shape
    mel_config: !ref <mel_config>
    max_length: 60
    mel_norm_mean: -2.5
    mel_norm_std: 6
    mel_padding: -5
    use_text: !ref <run_opts[use_textprefix]>
    use_bn: True
    bn_config: !ref <bn_config>
    mask_use_alignment: False
    umm_hop_size: 960

collator:
    !new:recipes.voicebox.datasets.voicebox_promptmel_dataset.VoiceBoxCollator
    tokenizer_pad: 16384
    mel_config: !ref <mel_config>
    mel_padding: -5
    max_crop_second: 30
    bn_config: !ref <bn_config>
    bn_padding: !ref <bn_config[bn_padding]>
    use_bn: True

# dataloader configuration
train_dataloader: !new:torch.utils.data.DataLoader
    dataset: !ref <train_dataset>
    num_workers: !ref <run_opts[num_workers]>
    batch_size: null
    collate_fn: !ref <collator>
    pin_memory: True

val_dataloader: !new:torch.utils.data.DataLoader
    dataset: !ref <val_dataset>
    num_workers: !ref <run_opts[num_workers]>
    batch_size: null
    collate_fn: !ref <collator>
    pin_memory: True

# datamodule configuration
pl_datamodule: !new:recipes.voicebox.lit_modules.DataModule
    train_dataloader: !ref <train_dataloader>
    val_dataloader: !ref <val_dataloader>

model_cls: !name:recipes.voicebox.diffusion_modules.llama_ldm.LlamaDiffusion
    hp: !ref <model_config>

optimizer_cls: !name:deepspeed.ops.adam.FusedAdam
    lr: !ref <run_opts[learning_rate]>
    weight_decay: !ref <run_opts[weight_decay]>
    betas: [0.9, 0.95]
    eps: 0.0000001

_steps_per_epoch: !apply:operator.length_hint [!ref <train_dataloader>]
_world_size: !apply:samantha.utils.envs.getenv_int ["WORLD_SIZE", 1]
scheduler_cls:
    !name:recipes.bark.lr_schedulers.musiclm_lr_scheduler.WarmupCosine
    init_lr: !ref <run_opts[learning_rate]>
    warmup_steps: 1000
    cycle_steps: 500000
    min_lr: !ref <run_opts[learning_rate]> * 0.1

# lightning model setting
pl_module:
    !new:recipes.voicebox.lit_modules.lit_diffusion_voicebox.VoiceBoxModule
    model_cls: !ref <model_cls>
    optimizer_cls: !ref <optimizer_cls>
    scheduler_cls: !ref <scheduler_cls>
    checkpointing: !ref <run_opts[checkpointing]>
    resume_ckpt_path: !ref <run_opts[resume_ckpt_path]>
    criterions: ["l1"]
    required_modules:
        vocoder_model:
            initializer: !name:recipes.voicebox.vocoder.wvae.init_vocoder_from_ts
            hpath: hdfs://haruna/home/byte_data_seed/lf_lq/speech/user/zhangshuo/bigmusic/assets/vocoder/20240112/wavevae_decoder.pt
    diffusion_sample_rate: !ref <mel_config[sampling_rate]>
    val_output_samples_dir: !ref <run_opts[val_output_samples_dir]>/<run_opts[log_name]>/<run_opts[version]>
    bn_config: !ref <bn_config>

logger:
    - !new:pytorch_lightning.loggers.TensorBoardLogger
      save_dir: !ref <run_opts[tensorboard_dir]>
      name: !ref <run_opts[log_name]>
      version: !ref <run_opts[version]>
    - !new:pytorch_lightning.loggers.wandb.WandbLogger
      save_dir: !ref <run_opts[tensorboard_dir]>
      project: !ref <run_opts[log_name]>
      name: !ref <run_opts[version]>

callbacks:
    - !new:samantha.callbacks.HDFSModelCheckpoint
      dirpath: !ref <run_opts[log_dir]>/<run_opts[log_name]>/<run_opts[version]>
      hdfs_path: !ref <run_opts[hdfs_path]>/<run_opts[log_name]>/<run_opts[version]>
      every_n_train_steps: !ref <run_opts[n_step_save]>
      mode: min
      save_last: True
      save_top_k: 20
      save_on_train_epoch_end: True
      verbose: True
      monitor: loss
      filename: "{epoch:02d}-{step}-{loss:.2f}"
    - !new:pytorch_lightning.callbacks.LearningRateMonitor
      logging_interval: "step"
    - !new:samantha.callbacks.timer.Timer

plugins:
    - !new:samantha.plugins.LargeTorchCheckpointIO

trainer: !new:pytorch_lightning.Trainer
    fast_dev_run: !ref <run_opts[fast_dev_run]>
    logger: !ref <logger>
    accelerator: !ref <run_opts[accelerator]>
    devices: "auto"
    num_nodes: !apply:os.getenv ["ARNOLD_WORKER_NUM", 1]
    deterministic: False # warning: setting to True can cause slowdowns
    enable_model_summary: True
    log_every_n_steps: 1000
    strategy: !ref <run_opts[strategy]>
    max_epochs: !ref <run_opts[num_epochs]>
    precision: !ref <run_opts[precision]>
    profiler: !ref <run_opts[profiler]>
    callbacks: !ref <callbacks>
    gradient_clip_val: 0.2
    benchmark: false
    accumulate_grad_batches: !ref <run_opts[accumulate_grad_batches]>
    plugins: !ref <plugins>
    sync_batchnorm: True
    limit_val_batches: !ref <run_opts[limit_val_batches]>
    val_check_interval: !ref <run_opts[val_check_interval]>

ckpt_path: null
