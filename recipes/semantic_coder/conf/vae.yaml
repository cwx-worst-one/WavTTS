---

################################################################################
##########                       BASIC CONFIGURATION                  ##########
################################################################################

ckpt_path: null

seed: 555
__set_seed: !apply:pytorch_lightning.seed_everything [!ref <seed>]
__set_float32_matmul_precision: !apply:torch.set_float32_matmul_precision ['high']

extra_params:
    sample_rate: 24000
    duration: 10
    feature_dim: 1024
    latent_dim: 16
    downsample_rates: [1, 1, 1, 1]
    upsample_rates: [1, 1, 1, 1]
    encoder_base_dim: 96
    decoder_base_dim: 2560
    semantic_layer_idx: 12
    loss_beta: 5e-4

# running parameters
run_opts:
    fast_dev_run: False
    log_dir: !apply:os.getenv ["ARNOLD_OUTPUT", "logs"]
    log_name: semantic_coder_vae
    version: mcc7m
    cache_dir: .module_cache/semantic_coder
    precision: "32"
    num_workers: 8
    accelerator: auto
    val_check_interval: 5000
    max_steps: 1000000
    batch_size: 100
    learning_rate: 5.0e-4  # Adjust according to batch size
    warmup_steps: 5000
    cycle_steps: 50000
    weight_decay: 0.0
    min_volume_threshold: 0.05
    loudness_ratio_threshold: 0.2
    shuffle_buffer: 300

################################################################################
##########                    TRAINING CONFIGURATION                  ##########
################################################################################

train_dataset: !new:recipes.musiclm.datasets.mcc.MCC7MDataset
    sample_rate: !ref <extra_params[sample_rate]>
    sample_duration: !ref <extra_params[duration]>
    min_volume_threshold: !ref <run_opts[min_volume_threshold]>
    loudness_ratio_threshold: !ref <run_opts[loudness_ratio_threshold]>
    meta_paths:
        - /mnt/bd/sami-mm-audios-shard1/metadata/15m_filter_trim_us_copy.txt
        - /mnt/bn/audio-diffusion/data/9m_aed/metalist.txt

train_dataloader: !new:torch.utils.data.DataLoader
    dataset: !ref <train_dataset>
    batch_size: !ref <run_opts[batch_size]>
    num_workers: !ref <run_opts[num_workers]>
    shuffle: True
    drop_last: True
    pin_memory: True

validation_dataset: !new:recipes.musiclm.datasets.karaoke.KaraokeDataset
    urls: "pipe:hdfs dfs -cat hdfs://harunava/home/byte_speech_sv/data/karaoke_for_singsong/shards-0131.tar"
    nodesplitter: !name:recipes.musiclm.datamodules.webdataset.return_self
    sample_rate: !ref <extra_params[sample_rate]>
    duration: !ref <extra_params[duration]>
    min_volume_threshold: !ref <run_opts[min_volume_threshold]>

validation_dataloader: !new:webdataset.WebLoader
    dataset: !ref <validation_dataset>
    shuffle: False
    batch_size: !ref <run_opts[batch_size]>
    num_workers: !ref <run_opts[num_workers]>
    pin_memory: True

pl_datamodule: !new:recipes.musiclm.datamodules.lit_datamodule.DataModule
    train_dataloader: !ref <train_dataloader>
    val_dataloader: !ref <validation_dataloader>

model_cls: !name:recipes.semantic_coder.models.vae.VAE
    feature_dim: !ref <extra_params[feature_dim]>
    latent_dim: !ref <extra_params[latent_dim]>
    downsample_rates: !ref <extra_params[downsample_rates]>
    upsample_rates: !ref <extra_params[upsample_rates]>
    encoder_base_dim: !ref <extra_params[encoder_base_dim]>
    decoder_base_dim: !ref <extra_params[decoder_base_dim]>

optimizer_cls: !name:torch.optim.AdamW
    lr: !ref <run_opts[learning_rate]>
    weight_decay: !ref <run_opts[weight_decay]>
    betas: [0.9, 0.96]
    eps: 0.00000001

scheduler_cls: !name:recipes.musiclm.optim.lr_scheduler.warmup_cosine_lr.WarmupCosine
    init_lr: !ref <run_opts[learning_rate]>
    warmup_steps: !ref <run_opts[warmup_steps]>
    cycle_steps: !ref <run_opts[cycle_steps]>
    min_lr: !ref <run_opts[learning_rate]> * 0.1

pl_module: !new:recipes.semantic_coder.modules.lit_module.VAE
    model_cls: !ref <model_cls>
    optimizer_cls: !ref <optimizer_cls>
    scheduler_cls: !ref <scheduler_cls>
    criterion_cls: !name:torch.nn.CrossEntropyLoss
    extra_params: !ref <extra_params>
    semantic_module: /mnt/bn/zongyu-lq/logs/best_rq/pgc600k_25hz_4x8192_522m/checkpoints/step=300000-tr_loss=4.2427-val_loss_0=3.9067.pt

logger:
    - !new:pytorch_lightning.loggers.tensorboard.TensorBoardLogger
      save_dir: !ref <run_opts[log_dir]>
      name: !ref <run_opts[log_name]>
      version: !ref <run_opts[version]>
      default_hp_metric: False

callbacks:
    - !new:pytorch_lightning.callbacks.ModelCheckpoint
      monitor: step
      save_top_k: 50
      every_n_train_steps: !ref <run_opts[val_check_interval]>
      mode: max
      filename: "{step:06d}-{tr_loss:.4f}-{val_loss_0:.4f}"
    - !new:pytorch_lightning.callbacks.LearningRateMonitor
      logging_interval: 'step'

plugins:
    - !new:samantha.plugins.torch_io.LargeTorchCheckpointIO

trainer: !new:pytorch_lightning.Trainer
    strategy: "ddp_find_unused_parameters_false"
    fast_dev_run: !ref <run_opts[fast_dev_run]>
    logger: !ref <logger>
    accelerator: !ref <run_opts[accelerator]>
    devices: 'auto'
    num_nodes: !apply:os.getenv ["ARNOLD_WORKER_NUM", 1]
    deterministic: False
    enable_model_summary: True
    log_every_n_steps: 100
    max_steps: !ref <run_opts[max_steps]>
    precision: !ref <run_opts[precision]>
    accumulate_grad_batches: 1
    val_check_interval: !ref <run_opts[val_check_interval]>
    check_val_every_n_epoch: null
    num_sanity_val_steps: 2
    callbacks: !ref <callbacks>
    plugins: !ref <plugins>
