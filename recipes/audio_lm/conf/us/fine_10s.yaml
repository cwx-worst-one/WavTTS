---

################################################################################
##########                       BASIC CONFIGURATION                  ##########
################################################################################

hp: !include:hparams_x480.yaml

seed: 2023
__set_seed: !apply:pytorch_lightning.seed_everything [!ref <seed>]

################################################################################
##########                    TRAINING CONFIGURATION                  ##########
################################################################################
run_opts:
    log_dir: /mnt/bn/zongyu-lq/logs
    fast_dev_run: False
    tensorboard_dir: !ref <run_opts[log_dir]>
    log_name: musiclm_fine_10s
    version: 3.2
    precision: 32
    accelerator: auto
    strategy: ddp_find_unused_parameters_false
    cache_dir: .module_cache/musiclm_fine_10s
    profiler: simple
    flash_attn: null
    num_epochs: 100
    batch_size: 1
    num_workers: 8
    learning_rate: 0.0003
    checkpointing: False
    hdfs_path: null
_checkpointing: !ref <run_opts[checkpointing]>

# gpt hyper-config
gpt_config: !new:transformers.GPT2Config
    vocab_size: 12289
    n_positions: 6000
    n_ctx: 6000
    n_embd: 1024
    n_layer: 24
    n_head: 16
    activation_function: gelu_new
    resid_pdrop: 0.1
    embd_pdrop: 0.0
    attn_pdrop: 0.1
    layer_norm_epsilon: 0.00001
    initializer_range: 0.02
    use_cache: !apply:operator.not_ [!ref <_checkpointing>]

train_dataset: !new:recipes.audio_lm.datasets.AudioLMDataset
    sample_rate: 24000
    sample_duration: 10
    meta_paths:
        - /mnt/bd/sami-mm-audios-shard1/metadata/15m_filter_trim_us_copy.txt

# dataloader configuration
train_dataloader: !new:torch.utils.data.DataLoader
    dataset: !ref <train_dataset>
    batch_size: !ref <run_opts[batch_size]>
    num_workers: !ref <run_opts[num_workers]>
    shuffle: True
    drop_last: True
    pin_memory: True

# datamodule configuration
pl_datamodule: !new:recipes.audio_lm.lit_modules.DataModule
    train_dataloader: !ref <train_dataloader>

# model configuration
model_cls: !name:recipes.audio_lm.modules.gpt.LanguageModel
    config: !ref <gpt_config>
    flash_attn: !ref <run_opts[flash_attn]>
    logit_num: 1024
    use_cond: False
    cond_dim: 128
    cond_type: linear

# training module configuration
optimizer_cls: !name:torch.optim.AdamW
    lr: !ref <run_opts[learning_rate]>
    weight_decay: 0.1
    betas: [0.9, 0.96]
    eps: 0.00000001

_steps_per_epoch: !apply:operator.length_hint [!ref <train_dataloader>]
_world_size: !apply:recipes.audio_lm.utils.env.getenv_int ["WORLD_SIZE", 1]
scheduler_cls: !name:recipes.audio_lm.lr_schedulers.musiclm_lr_scheduler.WarmupCosine
    init_lr: !ref <run_opts[learning_rate]>
    warmup_steps: 2000
    cycle_steps: 1000000
    min_lr: !ref <run_opts[learning_rate]> * 0.1

pl_module: !new:recipes.audio_lm.lit_modules.v3_1.musiclm.FineModule
    checkpointing: !ref <run_opts[checkpointing]>
    model_cls: !ref <model_cls>
    optimizer_cls: !ref <optimizer_cls>
    scheduler_cls: !ref <scheduler_cls>
    criterion_cls: !name:recipes.audio_lm.modules.MaskedCrossEntropy
    extra_hparams: !ref <hp>
    required_modules:
        mulan:
            - /mnt/bn/zongyu-lq/ckpts/mulan/mulan_149.pt
            - !name:recipes.audio_lm.requires.model_initializer.init_mulan
              cache_dir: !ref <run_opts[cache_dir]>
        mulan_centers:
            - /mnt/bn/zongyu-lq/ckpts/mulan/kmeans_minibatch_codebook-1024x12.npy
            - !name:recipes.audio_lm.requires.model_initializer.init_mulan_centers
              cache_dir: !ref <run_opts[cache_dir]>
        w2v:
            - /mnt/bn/zongyu-lq/ckpts/w2v/
            - !name:recipes.audio_lm.requires.model_initializer.init_w2v
              cache_dir: !ref <run_opts[cache_dir]>
        sound_stream:
            - /mnt/bn/zongyu-lq/ckpts/soundstream/190k
            - !name:recipes.audio_lm.requires.model_initializer.init_sound_stream
              cache_dir: !ref <run_opts[cache_dir]>

logger: !new:pytorch_lightning.loggers.TensorBoardLogger
    save_dir: !ref <run_opts[tensorboard_dir]>
    name: !ref <run_opts[log_name]>
    version: !ref <run_opts[version]>

callbacks:
    - !new:pytorch_lightning.callbacks.ModelCheckpoint
      every_n_train_steps: 1000
      mode: max
      save_last: True
      save_top_k: 10
      save_on_train_epoch_end: True
      verbose: True
      monitor: acc
      filename: "{epoch:02d}-{step}-{acc:.2f}"
    - !new:pytorch_lightning.callbacks.LearningRateMonitor
      logging_interval: 'step'
    - !new:recipes.audio_lm.callbacks.hdfs_callback.HdfsSavingCallback
      hdfs_path: !ref <run_opts[hdfs_path]>
      save_dir: !ref <run_opts[tensorboard_dir]>
      name: !ref <run_opts[log_name]>
      version: !ref <run_opts[version]>
      n_log: 1000

trainer: !new:pytorch_lightning.Trainer
    fast_dev_run: !ref <run_opts[fast_dev_run]>
    logger: !ref <logger>
    accelerator: !ref <run_opts[accelerator]>
    devices: 'auto'
    num_nodes: !apply:samantha.utils.envs.getenv_int ["ARNOLD_WORKER_NUM", 1]
    deterministic: False  # warning: setting to True can cause slowdowns
    enable_model_summary: True
    log_every_n_steps: 50
    strategy: !ref <run_opts[strategy]>
    max_epochs: !ref <run_opts[num_epochs]>
    precision: !ref <run_opts[precision]>
    profiler: !ref <run_opts[profiler]>
    callbacks: !ref <callbacks>
    gradient_clip_val: 1.0
    track_grad_norm: 2.0

ckpt_path: null
