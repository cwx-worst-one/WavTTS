---

################################################################################
##########                       BASIC CONFIGURATION                  ##########
################################################################################

ckpt_path: null

seed: 555
__set_seed: !apply:pytorch_lightning.seed_everything [!ref <seed>]
__set_float32_matmul_precision: !apply:torch.set_float32_matmul_precision ['high']
_checkpointing: False

extra_params:
    sample_rate: 24000
    duration: 10
    wav2vec_codebook_size: 1024
    wav2vec_frame_rate: 25
    mulan_codebook_size: 1024
    mulan_num_rvq: 12
    sft: True
    vad_conditioned: False
    dataset_return_keys: audio,text

# running parameters
run_opts:
    fast_dev_run: False
    log_dir: !apply:os.getenv ["ARNOLD_OUTPUT", "logs"]
    log_name: semantic_flash_llama
    version: mcc40m_mulan_text
    cache_dir: .module_cache/musiclm
    precision: "bf16-mixed"
    num_workers: 8
    accelerator: auto
    val_check_interval: 2000
    max_steps: 1000000
    batch_size: 64
    learning_rate: 2.0e-4  # Adjust according to batch size
    warmup_steps: 5000
    cycle_steps: 50000
    weight_decay: 0.01
    min_volume_threshold: 0.05
    loudness_ratio_threshold: 0.2
    shuffle_buffer: 256
    gradient_clip_val: 1.0
    seq_len: 400
    use_pipe: False

################################################################################
##########                    TRAINING CONFIGURATION                  ##########
################################################################################

# === Mulan Text Datasets ===

karaoke_dataset: !new:recipes.mulan.dataset.karaoke.KaraokeDataset
    seq_len: !ref <run_opts[seq_len]>
    resampled: True

mcc_gpt_dataset: !new:recipes.mulan.dataset.mcc.MCCGPTGenDataset
    seq_len: !ref <run_opts[seq_len]>
    resampled: True

aed_gpt_dataset: !new:recipes.mulan.dataset.mcc.AEDGPTGenDataset
    seq_len: !ref <run_opts[seq_len]>
    resampled: True

mcc_n2m_dataset: !new:recipes.mulan.dataset.mcc.MCCN2MDatasetApril
    seq_len: !ref <run_opts[seq_len]>
    resampled: True

everynoise_nonvocal_dataset: !new:recipes.mulan.dataset.mme.MMEDataset
    name: everynoise
    audio_type: nonvocal
    seq_len: !ref <run_opts[seq_len]>
    resampled: True
    use_pipe: !ref <run_opts[use_pipe]>

metacritic_nonvocal_dataset: !new:recipes.mulan.dataset.mme.MMEDataset
    name: metacritic
    audio_type: nonvocal
    seq_len: !ref <run_opts[seq_len]>
    resampled: True
    use_pipe: !ref <run_opts[use_pipe]>

human_labelled_vocal_dataset: !new:recipes.mulan.dataset.mme.MMEDataset
    name: human_labelled_5k
    audio_type: vocal
    seq_len: !ref <run_opts[seq_len]>
    resampled: True
    use_pipe: !ref <run_opts[use_pipe]>

human_labelled_nonvocal_dataset: !new:recipes.mulan.dataset.mme.MMEDataset
    name: human_labelled_5k_train
    audio_type: nonvocal
    seq_len: !ref <run_opts[seq_len]>
    resampled: True
    use_pipe: !ref <run_opts[use_pipe]>


train_datasets:
    karaoke: !ref <karaoke_dataset>
    mcc_gpt: !ref <mcc_gpt_dataset>
    aed_gpt: !ref <aed_gpt_dataset>
    mcc_n2m: !ref <mcc_n2m_dataset>
    meta_nonvocal: !ref <metacritic_nonvocal_dataset>
    every_nonvocal: !ref <everynoise_nonvocal_dataset>

selected_dataset_names: [mcc_gpt, aed_gpt, karaoke, mcc_n2m, meta_nonvocal, every_nonvocal]
weights: [0.2, 0.1, 0.2, 0.6, 0.05, 0.05]

train_dataset: !new:samantha.dataio.dataset.MultiIterableDataset
    datasets: !apply:recipes.mulan.dataset.utils.select_datasets
        - !ref <train_datasets>
        - !ref <selected_dataset_names>
    weights: !ref <weights>
    num_samples: 10_000_000
    seed: !ref <seed>

validation_dataset: !new:recipes.mulan.dataset.mme.MMEDataset
    name: human_labelled_5k_valid
    audio_type: nonvocal
    seq_len: !ref <run_opts[seq_len]>
    nodesplitter: !name:recipes.musiclm.datamodules.webdataset.return_self
    resampled: False
    use_pipe: !ref <run_opts[use_pipe]>

pl_datamodule: !new:recipes.musiclm.datamodules.webdataset.DataModule
    train_dataset: !ref <train_dataset>
    validation_dataset: !ref <validation_dataset>
    batch_size: !ref <run_opts[batch_size]>
    shuffle_buffer_size: !ref <run_opts[shuffle_buffer]>
    num_workers: !ref <run_opts[num_workers]>
    pin_memory: True
    return_dict: True
    train_keys: !ref <extra_params[dataset_return_keys]>
    val_keys: !ref <extra_params[dataset_return_keys]>
    predict_keys: !ref <extra_params[dataset_return_keys]>

model_config: !new:transformers.LlamaConfig
    vocab_size: !ref <extra_params[wav2vec_codebook_size]> + <extra_params[mulan_num_rvq]> * <extra_params[mulan_codebook_size]> + 1
    hidden_size: 1536
    intermediate_size: 4128
    num_hidden_layers: 24
    num_attention_heads: 12
    hidden_act: "silu"
    max_position_embeddings: !ref <extra_params[mulan_num_rvq]> + <extra_params[wav2vec_frame_rate]> * <extra_params[duration]>
    initializer_range: 0.02
    rms_norm_eps: 1e-6
    use_cache: False
    num_logits: !ref <extra_params[wav2vec_codebook_size]>

model_cls: !name:samantha.models.flash_llama.LlamaForCausalLM
    config: !ref <model_config>

optimizer_cls: !name:torch.optim.AdamW
    lr: !ref <run_opts[learning_rate]>
    weight_decay: !ref <run_opts[weight_decay]>
    betas: [0.9, 0.96]
    eps: 0.00000001

scheduler_cls: !name:recipes.musiclm.optim.lr_scheduler.warmup_cosine_lr.WarmupCosine
    init_lr: !ref <run_opts[learning_rate]>
    warmup_steps: !ref <run_opts[warmup_steps]>
    cycle_steps: !ref <run_opts[cycle_steps]>
    min_lr: !ref <run_opts[learning_rate]> * 0.1

pl_module: !new:recipes.musiclm.lightning.modules.SemanticModule
    checkpointing: !ref <_checkpointing>
    model_cls: !ref <model_cls>
    optimizer_cls: !ref <optimizer_cls>
    scheduler_cls: !ref <scheduler_cls>
    criterion_cls: !name:recipes.musiclm.lightning.modules.MaskedCrossEntropy
    extra_params: !ref <extra_params>
    seed_model: null
    required_modules:
        mulan:
            - hdfs://harunava/home/byte_speech_sv/weitsung.lu/mulan_exp/MuLan_large/gpt_all/checkpoints/mulan-step=036000-median_rank_0=127-kaggle.ckpt
            - !name:recipes.musiclm.requires.model_initializer.init_mulan
              cache_dir: !ref <run_opts[cache_dir]>
              version: "g4"
        mulan_centers:
            - hdfs://harunava/home/byte_speech_sv/dongguo/mulan/codebook/kmeans_minibatch_codebook-mulan1b_g4_mix_127-1024x12.npy
            - !name:recipes.musiclm.requires.model_initializer.init_mulan_centers
              cache_dir: !ref <run_opts[cache_dir]>
        wav2vec:
            - hdfs://harunava/home/byte_speech_sv/zongyu.yin/ckpts/w2v/2.1/semantic.jit.pt
            - !name:recipes.musiclm.requires.model_initializer.init_wav2vec
              cache_dir: !ref <run_opts[cache_dir]>
        semantic_centers:
            - hdfs://harunava/home/byte_speech_sv/zongyu.yin/ckpts/w2v/2.1/centroids_epoch_10.npy
            - !name:recipes.musiclm.requires.model_initializer.init_semantic_centers
              cache_dir: !ref <run_opts[cache_dir]>

logger:
    - !new:pytorch_lightning.loggers.tensorboard.TensorBoardLogger
      save_dir: !ref <run_opts[log_dir]>
      name: !ref <run_opts[log_name]>
      version: !ref <run_opts[version]>
      default_hp_metric: False
    - !new:pytorch_lightning.loggers.wandb.WandbLogger
      save_dir: !ref <run_opts[log_dir]>
      project: !ref <run_opts[log_name]>
      name: !ref <run_opts[version]>

callbacks:
    - !new:pytorch_lightning.callbacks.ModelCheckpoint
      monitor: step
      save_top_k: 20
      every_n_train_steps: !ref <run_opts[val_check_interval]>
      mode: max
      filename: "{step:06d}-{tr_loss:.4f}-{val_loss_0:.4f}"
    - !new:pytorch_lightning.callbacks.LearningRateMonitor
      logging_interval: 'step'

plugins:
    - !new:samantha.plugins.torch_io.LargeTorchCheckpointIO

trainer: !new:pytorch_lightning.Trainer
    strategy: "ddp_find_unused_parameters_false"
    fast_dev_run: !ref <run_opts[fast_dev_run]>
    logger: !ref <logger>
    accelerator: !ref <run_opts[accelerator]>
    devices: 'auto'
    num_nodes: !apply:os.getenv ["ARNOLD_WORKER_NUM", 1]
    deterministic: False
    enable_model_summary: True
    log_every_n_steps: 100
    max_steps: !ref <run_opts[max_steps]>
    precision: !ref <run_opts[precision]>
    accumulate_grad_batches: 1
    gradient_clip_val: !ref <run_opts[gradient_clip_val]>
    val_check_interval: !ref <run_opts[val_check_interval]>
    check_val_every_n_epoch: null
    num_sanity_val_steps: 2
    callbacks: !ref <callbacks>
    plugins: !ref <plugins>
