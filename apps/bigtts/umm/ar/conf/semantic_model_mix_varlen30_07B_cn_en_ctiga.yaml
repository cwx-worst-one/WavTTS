---

################################################################################
##########                       BASIC CONFIGURATION                  ##########
################################################################################

ckpt_path: null

seed: 1995
__set_seed: !apply:pytorch_lightning.seed_everything [!ref <seed>]
__set_float32_matmul_precision: !apply:torch.set_float32_matmul_precision ['high']

extra_params:
    sample_rate: 24000
    lyrics_codebook_size: 4096
    semantic_codebook_size: 8192
    semantic_frame_rate: 80
    semantic_type: bestrq
    mulan_embed_dim: 512
    hidden_size: 1536
    intermediate_size: 4352
    num_hidden_layers: 24
    num_attention_heads: 12
    pretrained_path: null

# running parameters
run_opts:
    fast_dev_run: False
    log_dir: ./logs
    hdfs_log_dir: None
    log_name: semantic_model_mulan_text_07B_cn
    version: v0
    cache_dir: .module_cache/musiclm
    precision: "16"
    num_workers: 8
    accelerator: auto
    max_steps: 1000000
    val_check_interval: 5000
    batch_size: 12
    learning_rate: 3.0e-4
    warmup_steps: 2000
    cycle_steps: 30000
    weight_decay: 0.05
    shuffle_buffer: 20   # 100
    gradient_clip_val: 1.0
    region: CN  # groupA, groupB, 1, 3, 4
    # offline umm token
    data_id: 319
    umm_token_freq: 40
    dataset_type: 2 # AUDIO = 1 UMM_TOKEN = 2
    target_token_key: umm_token

################################################################################
##########                    TRAINING CONFIGURATION                  ##########
################################################################################

pl_datamodule: !new:apps.bigtts.umm.ar.data.mix.MixWebDataModule
    sample_rate: !ref <extra_params[sample_rate]>
    shuffle_buffer_size: !ref <run_opts[shuffle_buffer]>
    num_workers: !ref <run_opts[num_workers]>
    normalize_audio: False
    tokenizer: sami
    min_duration: 2
    max_duration: 30
    weights: [0, 1, 0] # vocal, speech, instrumental
    batch_size: !ref <run_opts[batch_size]> * <pl_datamodule[max_duration]> * <run_opts[umm_token_freq]>
    region: !ref <run_opts[region]>
    dataset_type: !ref <run_opts[dataset_type]>
    data_id: !ref <run_opts[data_id]>
    umm_token_freq: !ref <run_opts[umm_token_freq]>
    target_token_key: !ref <run_opts[target_token_key]>
    skip_validation: True

model_config: !new:transformers.GPT2Config
    vocab_size: 1 # placeholder. Embedding table gets removed on load
    max_position_embeddings: 0
    use_cache: False
    n_positions: 0  # No absolute position embedding
    num_logits: !ref <extra_params[semantic_codebook_size]> + 2 # +2 for EOS
    n_embd: !ref <extra_params[hidden_size]>
    n_layer: !ref <extra_params[num_hidden_layers]>
    n_head: !ref <extra_params[num_attention_heads]>
    n_inner: !ref <extra_params[intermediate_size]>
    use_rms_norm: True
    activation_function: "swiglu"
    resid_pdrop: 0.0
    embd_pdrop: 0.0
    attn_pdrop: 0.0
    layer_norm_epsilon: 1e-6
    initializer_range: 0.02
    rescale_prenorm_residual: False
    rms_norm: True
    rotary_emb_fraction: 1.0
    rotary_emb_interleaved: True
    rotary_emb_compat: "default" #"byteformer"
    tie_word_embeddings: False
    qkv_proj_bias: False
    out_proj_bias: False
    mlp_fc1_bias: False
    mlp_fc2_bias: False
    use_flash_attn: True
    fused_bias_fc: True
    fused_mlp: False
    fused_dropout_add_ln: True
    residual_in_fp32: True

model_cls: !name:samantha.models.ctiga.gpt.GPTLMHeadModel
    config: !ref <model_config>

optimizer_cls: !name:torch.optim.AdamW
    lr: !ref <run_opts[learning_rate]>
    weight_decay: !ref <run_opts[weight_decay]>
    betas: [0.9, 0.96]
    eps: 0.00000001

scheduler_cls: !name:samantha.optim.lr_scheduler.warmup_cosine_lr.WarmupCosine
    init_lr: !ref <run_opts[learning_rate]>
    warmup_steps: !ref <run_opts[warmup_steps]>
    cycle_steps: !ref <run_opts[cycle_steps]>
    min_lr: !ref <run_opts[learning_rate]> * 0.1

pl_module: !new:apps.bigtts.umm.ar.lit_semantic_modules.SemanticModule_Valle
    checkpointing: False
    model_cls: !ref <model_cls>
    optimizer_cls: !ref <optimizer_cls>
    scheduler_cls: !ref <scheduler_cls>
    criterion_cls: !name:samantha.criterion.masked_loss.MaskedCrossEntropyV2
    extra_params: !ref <extra_params>
    # required_modules: 
        # bestrq:
            # US UMM-chroma stage 3
            # hpath: hdfs://haruna/home/byte_speech_sv/zhuo.chen1/umm/log/umm/umm_stage3_dw20-3-2_wordpiece_vq16384x32_ll_data182/checkpoints/step=040000.ckpt
            # hpath: /mnt/bn/mam/kainan/umm/ac-diff/step=040000_cn_en.ckpt
            # hpath: hdfs://haruna/home/byte_speech_sv/user/litang/umm_ar/25hz/logs/umm/umm_stage3_dw20-3-2_wordpiece_vq32768x32/checkpoints/step=040000.ckpt
            # hpath: hdfs://haruna/home/byte_speech_sv/user/litang/umm_ar/40hz/logs/umm/umm_stage3_dw20-3-2_wordpiece_chroma50_vq16384x32_EMAEntropy/checkpoints/step=145000.ckpt
            # initializer: !name:recipes.umm.requires.model_initializer.init_stage3
                # cache_dir: !ref <run_opts[cache_dir]>
        # # Mulan mix - 160
        # mulan:
        #     hpath: /mnt/bn/audio-diffusion/mulan/ongoing/mulan-step=014000-median_rank_1=160-kaggle.ckpt
        #     initializer: !name:recipes.musiclm.requires.model_initializer.init_mulan
        #         cache_dir: !ref <run_opts[cache_dir]>
        #         version: "g4"

logger:
    - !new:pytorch_lightning.loggers.tensorboard.TensorBoardLogger
      save_dir: !ref <run_opts[log_dir]>
      name: !ref <run_opts[log_name]>
      version: !ref <run_opts[version]>
      default_hp_metric: False
    - !new:pytorch_lightning.loggers.wandb.WandbLogger
      save_dir: !ref <run_opts[log_dir]>
      project: !ref <run_opts[log_name]>
      name: !ref <run_opts[version]>


# log_gradient_scaler: !new:recipes.musiclm.callbacks.loggers.LogGradientScalerCallback

callbacks:
    - !new:pytorch_lightning.callbacks.LearningRateMonitor
      logging_interval: 'step'
    - !new:samantha.callbacks.HDFSModelCheckpoint
      dirpath: !ref <run_opts[log_dir]>/<run_opts[log_name]>/<run_opts[version]>
      hdfs_path: !ref <run_opts[hdfs_log_dir]>/<run_opts[log_name]>/<run_opts[version]>
      monitor: accu
      save_top_k: 30
      every_n_train_steps: 10000
      mode: max
      filename: "{step:06d}-{accu:.2f}"

plugins:
    - !new:samantha.plugins.torch_io.LargeTorchCheckpointIO

profiler: !new:pytorch_lightning.profilers.PyTorchProfiler
    schedule: !apply:torch.profiler.schedule
        wait: 0
        warmup: 50
        active: 10
        repeat: 1
        skip_first: 100
    profile_memory: True

trainer: !new:pytorch_lightning.Trainer
    strategy: "ddp_find_unused_parameters_false"
    fast_dev_run: !ref <run_opts[fast_dev_run]>
    logger: !ref <logger>
    accelerator: !ref <run_opts[accelerator]>
    devices: 'auto'
    num_nodes: !apply:os.getenv ["ARNOLD_WORKER_NUM", 1]
    deterministic: False
    enable_model_summary: True
    log_every_n_steps: 500
    max_steps: !ref <run_opts[max_steps]>
    precision: !ref <run_opts[precision]>
    accumulate_grad_batches: 1
    gradient_clip_val: !ref <run_opts[gradient_clip_val]>
    val_check_interval: !ref <run_opts[val_check_interval]>
    check_val_every_n_epoch: null
    num_sanity_val_steps: 2  # skip validation sanity checks so we don't run into hdfs file close errors
    callbacks: !ref <callbacks>
    plugins: !ref <plugins>
    # profiler: !ref <profiler>
